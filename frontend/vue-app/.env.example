# 留空时，开发服务器使用 Vite 的 /api 代理；生产 Nginx 使用同源 /api。
# 仅在前端与 API 部署在不同地址时填写，例如 https://api.example.com。
VITE_API_BASE_URL=

# 本地 Vite 代理目标；Docker/Nginx 部署时不需要此变量。
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
